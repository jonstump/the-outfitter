{
  "name": "the-outfitter",
  "private": true,
  "version": "1.0.0",
  "description": "Hunt: Showdown loadout builder — frontend + backend",
  "engines": {
    "node": "^20"
  },
  "workspaces": [
    "client",
    "server",
    "desktop"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,yellow \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run test -w client && npm run test -w server && npm run test:scrape && npm run test -w desktop",
    "test:scrape": "node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "sharp": "^0.35.3"
  }
}
