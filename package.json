{
  "name": "the-outfitter",
  "private": true,
  "version": "1.0.0",
  "description": "Hunt: Showdown loadout builder — frontend + backend",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,yellow \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test:scrape-images": "node --test scripts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
